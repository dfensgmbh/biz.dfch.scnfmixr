# Description

* This folder contains the sound wav files for the audio interface.
* Each file consists of a prefix (describing the type), an infix (describing the state) and a suffix (indicating the language) delimited via a '.'.
* Audio format: 32000Hz, 16bit, Stereo, SE16_LF
 
